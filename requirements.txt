# ============================================================================
# Streamlit Community Cloud DEPLOY bağımlılıkları (HAFİF — torch/chroma YOK).
# Bu dosya streamlit_app.py + src/rag_cloud.py + app_cloud.py için yeterlidir.
#
# Ağır YEREL sürüm (app.py: torch + sentence-transformers + chromadb + reranker)
# için:  pip install -r requirements-local.txt
# ============================================================================
streamlit==1.56.0
pandas==3.0.2
voyageai==0.4.1
pinecone==9.1.0
rank-bm25==0.2.2
groq==1.2.0
openai>=1.50.0
python-dotenv==1.2.2

# Bulut API (app_cloud.py) — Streamlit Cloud kullanmaz ama hafif, kalsın
fastapi==0.138.2
uvicorn==0.46.0
